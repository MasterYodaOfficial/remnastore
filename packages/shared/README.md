# Общий пакет

Пакет для общих DTO, типов и схем между `API`, `Bot`, `Web` и будущей `Admin` частью.

Если тип используется больше чем в одном приложении, его имеет смысл держать здесь, а не копировать по сервисам.
