# Общий пакет

`packages/shared` — место для общих типов, DTO и схем, которые используются больше чем в одном сервисе.

Если сущность нужна одновременно в `api`, `bot`, `web` или `admin`, ее стоит держать здесь.
