# Локали

Пакет с пользовательскими текстами для:

- `bot`
- `web`
- общих строк

Файлы:

- `common.json` — общие подписи
- `bot.json` — тексты бота
- `web.json` — тексты пользовательского кабинета

Формат — `JSON` с плейсхолдерами вида `{name}`.
